# instructions

This directory contains instructions to setup the open-source tools.  It is written in LaTeX

## Environment Setup

If you install the open-source VLSI tools locally on your machine, the
`setup.sh` script can be used to configure the required environment
variables and tool paths for the design flow.

Typical usage:

```bash
source setup.sh


